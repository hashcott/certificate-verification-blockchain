export enum AccountStatus {
	PENDING = 'pending',
	VERIFIED = 'verified',
	BANNED = 'banned'
}

export enum CertificationStatus {
	PENDING = 'pending',
	VERIFIED = 'verified',
	BANNED = 'banned'
}

export enum DegreeClassfication {
	EXCELLENT = 'excellent',
	GOOD = 'good'
}
