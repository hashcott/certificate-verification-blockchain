export enum AccountStatus {
	PENDING = 'pending',
	VERIFIED = 'verified',
	BANNED = 'banned'
}

export enum CertificationStatus {
	PENDING = 'pending',
	VERIFIED = 'verified',
	BANNED = 'banned'
}

export enum DegreeClassification {
	Excellent = 1,
	Good = 2,
	FairlyGood = 3,
	Average = 4
}
